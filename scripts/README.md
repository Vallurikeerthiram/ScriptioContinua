# Automation Scripts

This folder contains PowerShell scripts for automating repetitive tasks.

## Scripts

- **`add_avg_score_sheet.ps1`**: A PowerShell script used to add a worksheet with average scores to the results workbooks.
